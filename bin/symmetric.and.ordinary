#! /bin/sh
COUNT=`ca-uniq -v 2>&1 | grep isomorphic | wc | awk '{ print $1; }'`
if [ "$COUNT" = "1" ]; then
  ./bin/symmetric.and.ordinary.sub.refleiso
else
  ./bin/symmetric.and.ordinary.sub.mirror
fi
